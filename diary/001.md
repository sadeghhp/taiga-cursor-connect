# Diary 001

**2026-05-22**

## Unit tests (Taiga MCP plan)

- Added `schemas.test.ts`, `errors.test.ts`, expanded `taiga-client.test.ts` (51 tests, all pass).
- Harness: test glob, `test:watch`, `setClientForTests`, runtime env read for `requireConfig`, README Testing section, `tsconfig` `types: ["node"]`.
- Extracted `formatTaigaError` to `src/errors.ts`; wired `server.ts` to import it.
- Skipped optional 429 interceptor test (no new deps).

## MCP tool audit (v0.4.0)

- Claim "no create" is false in source since v0.3.0; Cursor MCP cache showed only 10 tools (stale image).
- Added discovery tools: milestones, statuses, members, tasks, issues; `get_epic`, `create_epic`; create optional status/milestone/assignee.
- 25 MCP tools total; rebuild Docker + restart Cursor to refresh tool list.

## Exchange R1 enhancement (v0.5.0)

- Modularized: `http/client`, `resolvers`, `trimmers`, `patch-builders`, `points`, `plan-sync`.
- 46 MCP tools: milestone CRUD, epic list/update/comment, bulk CSV sync, backlog/sprint order, archive/delete, list filters/pagination, tags/epicId/unassign on create/update.
- Docs: `docs/exchange-r1-bootstrap.md`, `docs/bulk-sync-example.md`, `scripts/smoke-mcp-tools.ts`, sample CSV fixture.
- 56 unit tests pass.
